/** Injected by vite.config.ts from @underlying/core's package.json version. */
declare const __CORE_VERSION__: string

/** Injected by vite.config.ts: each package's real version, read at build time. */
declare const __PKG_VERSIONS__: Record<string, string>
